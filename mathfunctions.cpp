#include <iostream>

int main() {
    double x = 3;
    double y = 4;
    double z;

    z= std::max(x, y);
    //min : for minumun valur return
    //pow(x,y): y goes in power of x
    //sqrt(y): returns root
    //abs(x): HOw far is thw value from zero (absolute)
    //same as c language


    std::cout << z;

    return 0;
}